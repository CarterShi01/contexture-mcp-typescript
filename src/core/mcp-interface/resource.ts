/**
 * Compatibility exports for SDK-neutral Resource declaration data owned by
 * foundation. An MCP adapter projects these facts but does not redefine them.
 */
export type { Resource, ResourceDeclaration } from '../foundation/publications.js';
