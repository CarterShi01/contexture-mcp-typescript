/**
 * Contexture's language-native authoring surface.
 *
 * The package is still a scaffold and is deliberately private until the
 * cross-language conformance contract is implemented.
 */
export * from './core/index.js';
